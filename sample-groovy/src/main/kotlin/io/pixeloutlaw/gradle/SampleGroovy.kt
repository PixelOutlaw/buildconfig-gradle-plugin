package io.pixeloutlaw.gradle

import io.pixeloutlaw.gradle.sample_groovy.BuildConfig

fun main(args: Array<String>) {
    println("BuildConfig.NAME: ${BuildConfig.NAME}")
    println("BuildConfig.VERSION: ${BuildConfig.VERSION}")
}
